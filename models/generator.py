# TODO: Implement ResNetGenerator (mask-conditioned)
# input_nc = 2 (TPAF + Mask) or 3 (NADH + FAD + Mask)
# output_nc = 3 (RGB H&E)
# norm_type = instance (required; never BatchNorm with batch_size=1)
# mask_injection: early_fusion | multi_scale
