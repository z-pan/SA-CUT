# TODO: Inference entry point.
# Usage: python scripts/test.py --config configs/experiment_full.yaml --checkpoint <path>
# CRITICAL: mask must come from the same AFN-DeSeg checkpoint used during training.
