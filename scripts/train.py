# TODO: Training entry point.
# Usage: python scripts/train.py --config configs/experiment_full.yaml [--set key=value ...]
# Loads YAML config, applies CLI overrides, instantiates SACUTTrainer, runs training.
